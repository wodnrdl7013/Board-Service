spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:board-mysql}:${DB_PORT:3306}/${DB_NAME:board}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate   # 🔹 prod에서도 자동생성 금지
    properties:
      hibernate:
        format_sql: true
        show_sql: false
  jackson:
    time-zone: Asia/Seoul

  flyway:
    enabled: true
    locations: classpath:db/migration
    encoding: UTF-8
    # EC2 DB도 이번에 싹 밀고 새로 만들 거면 baseline-on-migrate 필요 없음

jwt:
  secret: ${JWT_SECRET}
  access-token-validity-ms: ${JWT_ACCESS_TOKEN_VALIDITY_MS:3600000}

app:
  aws:
    s3:
      bucket: ${APP_AWS_S3_BUCKET}
      region: ${APP_AWS_S3_REGION}
      base-url: ${APP_AWS_S3_BASE_URL}
