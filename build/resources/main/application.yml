spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # 🔹 이제부터 DDL 자동 생성/수정 금지, 스키마 검증만
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    encoding: UTF-8
    # 새 DB 기준으로 갈 거니까 baseline-on-migrate는 안 쓴다.

springdoc:
  swagger-ui:
    path: /swagger-ui

jwt:
  secret: "ZmFrZV9zZWNyZXRfZm9yX2JvYXJkX3NlcnZpY2Vfc3VwZXJfMTIzNDU2Nzg5MA=="
  access-token-validity-ms: 3600000  # 1시간

app:
  aws:
    s3:
      bucket: board-service-jaewook-files
      region: ap-northeast-2
      base-url: https://board-service-jaewook-files.s3.ap-northeast-2.amazonaws.com
